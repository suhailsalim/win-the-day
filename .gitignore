# macOS
.DS_Store

# Xcode
/build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# OpenWolf (per-developer project intelligence; regenerate with `openwolf init` / `openwolf scan`)
.wolf/

# Claude Code: hooks/settings are per-developer, but project skills and rules are shared.
.claude/*
!.claude/skills/
!.claude/rules/

# Secrets — never commit. API keys live in the device Keychain at runtime, not in the repo.
*.key
secrets.plist

# MkDocs build output
site/
